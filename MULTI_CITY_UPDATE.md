# 多城市监控更新说明

## 更新时间
2026-03-06

## 更新内容

### ✅ 已完成的功能

#### 1. 多城市监控
- 从单城市扩展到 **44 个城市** 同时监控
- 支持逗号分隔的城市列表配置
- 自动遍历所有城市并聚合结果

#### 2. 预警类型过滤
- 新增预警类型过滤功能
- **仅监控影响物流运营的关键天气**：
  - 台风
  - 雪
  - 寒潮
  - 冰雹
  - 大雾
- 自动过滤其他不相关预警（暴雨、大风、道路结冰等）

#### 3. HTML 报告优化
- 标题显示监控城市数量
- 地图支持多城市标记
- 预警信息按城市分组展示
- 显示每个城市的预警数量

#### 4. 飞书通知优化
- 支持多城市预警汇总
- 显示有预警的城市列表
- 最多显示 10 条预警详情
- 剩余预警数量提示

---

## 监控城市列表（44个）

鞍山、蚌埠、包头、保定、北京、常德、常州、成都、大连、大庆、东莞、鄂尔多斯、佛山、福州、阜阳、赣州、广州、贵阳、哈尔滨、海口、邯郸、杭州、合肥、衡阳、驻马店、周口、宝鸡、德州、通辽、滨州、平顶山、郴州、廊坊、泰安、衢州、曲靖、枣庄、日照、丽水、承德、东营、齐齐哈尔、聊城、临汾

---

## 配置说明

### .env 文件配置

```env
# 心知天气 API Key
SENIVERSE_API_KEY=your_api_key_here

# 监控城市列表（逗号分隔）
CITY=鞍山,蚌埠,包头,保定,北京,常德,常州,成都,大连,大庆,东莞,鄂尔多斯,佛山,福州,阜阳,赣州,广州,贵阳,哈尔滨,海口,邯郸,杭州,合肥,衡阳,驻马店,周口,宝鸡,德州,通辽,滨州,平顶山,郴州,廊坊,泰安,衢州,曲靖,枣庄,日照,丽水,承德,东营,齐齐哈尔,聊城,临汾

# 监控的预警类型（逗号分隔）
ALERT_TYPES=台风,雪,寒潮,冰雹,大雾

# 飞书 Webhook
FEISHU_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx

# GitHub Pages URL
REPORT_URL=https://belongialiest-coder.github.io/weather/
```

---

## 测试结果

### 测试时间
2026-03-06

### 测试数据
- **监控城市**: 44 个
- **原始预警数**: 7 条
- **过滤后预警数**: 2 条
- **过滤掉**: 5 条不相关预警

### 保留的预警
- **蚌埠**: 大雾 - 黄色预警
- **保定**: 大雾 - 黄色预警

### 过滤掉的预警
- 大连: 暴雨、道路结冰等 4 条
- 宝鸡: 暴雨 1 条

---

## 主要代码变更

### 1. weather_alert.py

#### load_config() 函数
- 新增城市列表解析
- 新增预警类型解析

#### filter_alarms_by_type() 函数（新增）
- 根据预警类型关键词过滤
- 记录过滤日志

#### render_html() 函数
- 重构为支持 cities_data 数组
- 计算总预警数

#### build_feishu_card() 函数
- 支持多城市预警汇总
- 显示有预警的城市列表

#### main() 函数
- 完全重写为多城市迭代架构
- 添加过滤前后的统计

### 2. template.html

#### 标题部分
```html
<title>气象预警中心 - 监控 {{ cities_data|length }} 个城市</title>
```

#### 头部信息
```html
<div class="location">监控 {{ cities_data|length }} 个城市</div>
```

#### 更新时间
```html
更新时间: {{ update_time }} | 总预警数: {{ total_alarms }} 条 | 监控类型: 台风、雪、寒潮、冰雹、大雾
```

#### ECharts 地图
- 支持多城市标记
- 根据预警等级显示不同颜色

#### 预警展示
- 按城市分组
- 显示每个城市的预警数量

### 3. .env.example

- 添加多城市配置示例
- 添加 ALERT_TYPES 配置说明

---

## 使用方法

### 本地测试
```bash
cd weather-alert-bot
py weather_alert.py
```

### GitHub Actions 部署

1. 更新 GitHub Secrets 中的配置：
   - `CITY`: 更新为 44 个城市列表
   - `ALERT_TYPES`: 台风,雪,寒潮,冰雹,大雾

2. 推送代码到 GitHub：
```bash
git add .
git commit -m "feat: 支持44个城市监控和预警类型过滤"
git push
```

3. GitHub Actions 会自动运行并部署到 Pages

---

## 数据流程

```
1. 加载配置（44个城市 + 5种预警类型）
   ↓
2. 遍历每个城市
   ├─ 调用心知天气 API
   ├─ 提取预警信息
   ├─ 应用类型过滤
   └─ 存储城市数据
   ↓
3. 生成 HTML 报告
   ├─ ECharts 地图标记所有城市
   ├─ 按城市分组显示预警
   └─ 统计总预警数
   ↓
4. 发送飞书通知
   ├─ 汇总有预警的城市
   ├─ 显示最多 10 条预警
   └─ 提供查看详情按钮
```

---

## 性能考虑

- **API 调用**: 44 次查询（每城市 1 次）
- **预计耗时**: 约 1-2 分钟（含重试机制）
- **API 配额**: 注意心知天气免费版限制

---

## 下一步优化建议

1. ✅ 多城市监控（已完成）
2. ✅ 预警类型过滤（已完成）
3. 📋 考虑添加城市坐标数据库（提升地图准确性）
4. 📋 优化 API 调用（并发请求或批量查询）
5. 📋 添加历史预警趋势分析

---

## 相关文档

- README.md - 项目使用说明
- quick-check.md - 快速检查清单
- .env.example - 配置文件模板

---

**创建时间**: 2026-03-06
**创建者**: Claude Code
**版本**: 2.0 - 多城市监控版
